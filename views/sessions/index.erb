<h1>Sessions</h1>

<% @sessions.each do |session| %>
  <div class="session">
    <p><%= session.title %></p>
    <p><a href="/sessions/<%= session.id %>" >View</a></p>
  </div>
<% end %>

<a href="/sessions/new">Create new session</a>
